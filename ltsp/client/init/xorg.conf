# Generated by `ltsp init`, see man:ltsp(8) man:xorg.conf(5)

Section            "Device"
    Identifier     "Configured Video Device"
    # If the default driver doesn't work, try "vesa" or "modesetting"
    # Driver       "vesa"
EndSection

Section            "Monitor"
    Identifier     "Configured Monitor"
    # If the monitor timings (EDID) aren't detected, specify them below
    # HorizSync    28.0-87.0
    # VertRefresh  43.0-87.0

    # PreferredMode requires RandR 1.2, which vesa doesn't implement.
    # For consistency, use "Modes" under the "Display" SubSection in all cases.
    # Option       "PreferredMode" "1024x768"

    # A custom resolution can be specified here
    # Modeline     "1024x768_85.00"   94.50  1024 1096 1200 1376  768 771 775 809 -hsync +vsync
EndSection

Section            "Screen"
    Identifier     "Default Screen"
    Monitor        "Configured Monitor"
    Device         "Configured Video Device"
    SubSection     "Display"
        # Virtual can be used to blacklist higher resolutions.
        # Virtual  1024 768

        # The list of available modes
        # Modes    "1024x768" "800x600" "640x480"
    EndSubSection
EndSection
